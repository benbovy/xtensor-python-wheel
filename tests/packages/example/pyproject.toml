[build-system]
requires = [
    "scikit_build_core[pyproject]",
    "pybind11",
    "numpy",
]
build-backend = "scikit_build_core.build"

[project]
name = "example"
version = "0.0.1"
requires-python = ">=3.9"
dependencies = ["numpy"]
